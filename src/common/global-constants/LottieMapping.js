import doctorAnimation from '../../assets/lottie-files/doctor-patient.json';
import heartbeat from '../../assets/lottie-files/heartbeat.json';
import appointment from '../../assets/lottie-files/appointment.json';
import success from '../../assets/lottie-files/success.json';

const LOTTIE_MAPPING = {
  DOCTOR: doctorAnimation,
  HEARTBEAT: heartbeat,
  APPOINTMENT: appointment,
  SUCCESS: success,
};

export default LOTTIE_MAPPING;
